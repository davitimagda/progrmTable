class Tmima {
  constructor(name, taxi, type, subTmimaOf) {
    this.name = name;
    this.taxi = taxi; // Α, Β, Γ
    this.type = type; //GP (γενική παιδεία), OP(ομάδα προσ.), FL(ξένες γλώσσες)
    this.subTmimaOf = subTmimaOf;
  }
}

export default Tmima;
